# Offline training/ingestion only - never installed in the serving container.
# Runtime inference deps (scikit-learn, joblib) stay in requirements.txt.
-r requirements.txt
pandas>=2.2.3
kaggle==1.6.17
pyyaml==6.0.2
# CPU-only PyTorch for the §4 calorie-model benchmark (docs/ML_SPEC.md) - not a
# runtime/serving dependency, training-only. Left unpinned (like pandas above):
# both packages lag in publishing wheels for new Python releases, and a pinned
# version with no matching wheel forces pip into a from-source build that fails
# without a full C/C++ toolchain.
torch
